apiVersion: v1
kind: Service
metadata:
  name: talosdeck
  labels:
    app: talosdeck
    app.kubernetes.io/name: talosdeck
    app.kubernetes.io/part-of: talosdeck
spec:
  type: NodePort
  selector:
    app: talosdeck
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: 32000
      protocol: TCP
