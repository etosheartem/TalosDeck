apiVersion: v1
kind: Service
metadata:
  name: {{ include "talosdeck.fullname" . }}
  labels:
    {{- include "talosdeck.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "talosdeck.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
