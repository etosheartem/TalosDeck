<<<BUG K8S-01>>>
**Статус:** ✅ Исправлено
**Что было:** Корневая причина CRITICAL-краша — не расхождение имён (`node`/`podIp` уже соответствуют реальности), а `omitempty` на `NodeIP`/`PodIP`: для Pending-подов (пустой `HostIP`/`PodIP`) поле целиком пропадало из JSON, и фронтенд получал `undefined` вместо пустой строки → `TypeError: Cannot read properties of undefined`.
**Что сделано:** `internal/k8s/models.go:10,19` — убран `omitempty` у `NodeIP` (`json:"nodeIp"`) и `PodIP` (`json:"podIp"`), теперь эти ключи всегда присутствуют в ответе (пустой строкой, если IP ещё не назначен). Имена полей (`node`, `nodeIp`, `podIp`, `readyContainers` как `int`) сознательно НЕ переименованы под ожидания фронтенда (`nodeName`/`ip`/строка) — это зафиксированный, стабильный Go-контракт; маппинг на сторону фронтенда — задача другого агента. Итоговая JSON-схема ниже в общем ответе.
**Проверка:** build/vet/test ок. Отдельного теста на JSON-сериализацию не добавлял (тривиальный тег структуры, не логика).
<<<END>>>

<<<BUG K8S-02>>>
**Статус:** ✅ Исправлено
**Что было:** `ListPods` запрашивал все поды namespace без `FieldSelector` и фильтровал по `nodeFilter` в Go-цикле после получения полного списка из etcd.
**Что сделано:** `internal/k8s/client.go:92-96` — при непустом `nodeFilter` в `metav1.ListOptions.FieldSelector` подставляется `fields.OneTermEqualSelector("spec.nodeName", nodeFilter).String()`, фильтрация теперь идёт на сервере API. Ручной цикл-фильтр убран.
**Проверка:** build/vet/test ок.
<<<END>>>

<<<BUG K8S-03>>>
**Статус:** ✅ Исправлено
**Что было:** Для `namespace == "all"` был сброс в `""`, а для `nodeFilter == "all"` — не было, из-за чего `?node=all` уходил в `FieldSelector` как буквальное имя ноды `"all"` и всегда давал пустой список.
**Что сделано:** `internal/k8s/client.go:88-90` — добавлен симметричный сброс `if nodeFilter == "all" { nodeFilter = "" }` перед построением `FieldSelector`.
**Проверка:** build/vet/test ок; покрыто тестом `TestListPodsNilGuard`? нет — прямого теста на "all" нет юнит-тестом (нужен фейковый clientset с данными; см. ниже про K8S-04/05, где протестирована лишь чистая функция `podStatus`). Логика тривиальна (одна строка симметрична существующей для namespace), сочтено достаточным ручной проверкой кода.
<<<END>>>

<<<BUG K8S-04>>>
**Статус:** ✅ Исправлено
**Что было:** Статус пода вычислялся только по `ContainerStatuses[].State.Waiting.Reason`; `Terminated` состояние (`OOMKilled`, `Error`) игнорировалось, статус оставался `"Running"`.
**Что сделано:** `internal/k8s/client.go:166-201` — новая функция `podStatus`/`containerIssueReason`: сначала ищется `Waiting.Reason`, затем `Terminated` с `ExitCode != 0` (нормальное завершение с `ExitCode == 0` статусом-проблемой не считается).
**Проверка:** build/vet/test ок; тест `TestPodStatus` (кейс "terminated with non-zero exit surfaces (OOMKilled)").
<<<END>>>

<<<BUG K8S-05>>>
**Статус:** ✅ Исправлено
**Что было:** `InitContainerStatuses` не учитывались ни в счётчике рестартов, ни в статусе — падения init-контейнеров были невидимы.
**Что сделано:** `internal/k8s/client.go:115-117` — рестарты init-контейнеров суммируются в общий `restartCount`. `internal/k8s/client.go:171-173` — `podStatus` сначала проверяет `InitContainerStatuses` через `containerIssueReason` и при проблеме возвращает `"Init:" + reason` (аналог поведения `kubectl get pods`).
**Проверка:** build/vet/test ок; тест `TestPodStatus` (кейс "init container failure surfaces with Init: prefix").
<<<END>>>

<<<BUG K8S-06>>>
**Статус:** ⚠️ Частично (требует правки вне `internal/k8s/`)
**Что было:** Бэкенд ждёт k8s API до 10 сек (`restConfig.Timeout`), фронтенд обрывает запрос через 3 сек и подставляет MOCK-данные — рассинхрон таймаутов.
**Что сделано:** В `internal/k8s/` изменений не делал — 10-секундный таймаут в `client.go:38` оставлен как разумное значение per правилам задачи (не трогать `web/`, `internal/api/`). **Требуется у другого агента:** увеличить таймаут запроса в `web/src/api/index.ts:942` минимум до значения ≥ 10 сек (или добавить настраиваемый таймаут/AbortController с запасом), чтобы не срабатывал mock-фоллбэк на медленном, но живом кластере.
**Проверка:** build/vet/test ок (изменений в файле нет, не могло сломаться).
<<<END>>>

<<<BUG K8S-07>>>
**Статус:** ⚠️ Частично (только пагинация; informer/кэш — YAGNI)
**Что было:** `ListPods`/`ListNamespaces` делали один uncached `List` без `Limit`/`Continue`, вытягивая всю коллекцию из etcd за раз.
**Что сделано:** `internal/k8s/client.go:92,100-147` (`ListPods`) и `client.go:209-230` (`ListNamespaces`) — добавлен `Limit: listPageSize` (500, `client.go:20`) и цикл по `Continue`-токену, страницы агрегируются в единый результат прозрачно для вызывающего кода. Это снижает пиковую нагрузку на etcd за один List без изменения публичного API.
Informer/client-side cache сознательно НЕ добавлен: это отдельная архитектурная фича (watch-based кэш, инвалидация, доп. горутины/жизненный цикл), непропорциональная нынешнему масштабу кластера (домашний Talos-кластер, дашборд с редкими ручными обновлениями, не десятки RPS). Добавлять её сейчас — оверинжиниринг; имеет смысл только если профилирование покажет реальную нагрузку на etcd/API-сервер от дашборда.
**Проверка:** build/vet/test ок.
<<<END>>>

<<<BUG K8S-08>>>
**Статус:** ✅ Исправлено
**Что было:** `ListPods` и `ListNamespaces` не проверяли `m == nil || m.clientset == nil` перед обращением к `m.clientset...`, что даёт панику вместо ошибки.
**Что сделано:** `internal/k8s/client.go:80-82` (`ListPods`) и `client.go:205-207` (`ListNamespaces`) — добавлена guard-проверка в начале метода с возвратом `fmt.Errorf("kubernetes client is not initialized")`.
**Проверка:** build/vet/test ок; тест `TestListPodsNilGuard` (nil-менеджер и менеджер с nil `clientset`).
<<<END>>>

<<<BUG K8S-09>>>
**Статус:** ✅ Исправлено
**Что было:** Хардкод абсолютного пути `/home/artem/laba-kuber/kubeconfig` как fallback, ручной `os.Stat`/`clientcmd.BuildConfigFromFlags`, без `rest.InClusterConfig()`.
**Что сделано:** `internal/k8s/client.go:22-75` — резолвинг конфига переписан на стандартный `clientcmd.NewDefaultClientConfigLoadingRules()` + `NewNonInteractiveDeferredLoadingClientConfig` (сама библиотека клиент-го обходит: explicit path → env `KUBECONFIG` → `~/.kube/config`), при неудаче — `rest.InClusterConfig()`, и только затем (последний резерв) — переданный `kubeconfigBytesProvider` (talos raw kubeconfig, как и раньше). Хардкод пути и ручная логика поиска файла удалены.
**Проверка:** build/vet/test ок.
<<<END>>>
